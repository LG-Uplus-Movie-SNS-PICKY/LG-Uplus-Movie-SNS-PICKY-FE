import { Notification } from "../../constants";
interface NotificationRanderProps {
    title: string;
    section: Notification[];
}
declare function NotificationRander({ title, section }: NotificationRanderProps): import("@emotion/react/jsx-runtime").JSX.Element;
export default NotificationRander;
