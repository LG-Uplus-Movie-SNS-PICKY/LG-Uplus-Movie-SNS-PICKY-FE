interface MovieFooterProps {
    year: string;
    production: string;
    age: string;
    genre: string;
}
declare const MovieFooter: ({ year, production, age, genre }: MovieFooterProps) => import("@emotion/react/jsx-runtime").JSX.Element;
export default MovieFooter;
